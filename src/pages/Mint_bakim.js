import { useEffect } from "react";

const Mint = () => {
  useEffect(() => {
    window.scroll(0, 0);
  }, []);
  return (
    <div>

Mint is temporarily disabled due to ipfs gateway issue.
    </div>
  );
};

export default Mint;
